package gallia
package atoms

// ===========================================================================
package object utils {
  private[utils] type OObj = Option[Obj]
  private[utils] type OVle = Option[Vle]
}

// ===========================================================================